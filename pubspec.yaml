name: proballdev
description: "Wicked Rolling Ball Pro — AI-powered IoT pet toy companion app. Runs fully without hardware. BLE-ready."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  fl_chart: ^0.69.0
  shared_preferences: ^2.3.3
  flutter_secure_storage: ^9.2.2
  flutter_web_auth_2: ^4.0.0
  http: ^1.2.2
  image_gallery_saver: ^2.0.3
  permission_handler: ^11.3.1
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
